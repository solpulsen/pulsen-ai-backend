# ─── Supabase ─────────────────────────────────────────────────────────────────
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_ANON_KEY=<your-anon-key>

# NOTE: SUPABASE_SERVICE_ROLE_KEY is intentionally excluded from this file.
# It must ONLY be set in your secure runtime environment (e.g. Render/Railway secrets).

# ─── Embedding Provider ──────────────────────────────────────────────────────
# "openai"   = text-embedding-3-small (1536 dims, requires OPENAI_API_KEY)
# "fulltext" = Postgres fulltext search (BM25, no API key needed)
EMBEDDING_PROVIDER=fulltext

# ─── Chat Model ───────────────────────────────────────────────────────────────
CHAT_MODEL=gpt-4.1-mini

# ─── App ──────────────────────────────────────────────────────────────────────
APP_ENV=development

# ─── SECURITY WARNING ────────────────────────────────────────────────────────
# OPENAI_API_KEY must NEVER be in this file. Set in runtime secrets only.
# SUPABASE_SERVICE_ROLE_KEY must NEVER be in this file. Set in runtime secrets only.
# API_SECRET_KEY must NEVER be in this file. Set in runtime secrets only.
